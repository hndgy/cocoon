{
  "name": "cocoon",
  "version": "0.1.0",
  "description": "Cocoon — Claude's cozy isolated shell",
  "main": "dist/index.js",
  "bin": {
    "cocoon": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "claude",
    "docker",
    "container",
    "sandbox"
  ],
  "license": "MIT",
  "type": "module",
  "dependencies": {
    "commander": "^14.0.3",
    "dockerode": "^4.0.10"
  },
  "devDependencies": {
    "@types/dockerode": "^4.0.1",
    "@types/node": "^25.5.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.0"
  }
}
